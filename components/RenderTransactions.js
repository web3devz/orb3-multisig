import React from "react";
import CustomizedTables from "./TransactionsTable";

const RenderTransactions = (props) => {
  return (
    <div>{/* <CustomizedTables transactions={props.transactions} /> */}</div>
  );
};

export default RenderTransactions;
